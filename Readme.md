Edited README.md

Done! The [README.md](file:///Users/shikhar/Documents/antigravity/nifty-franklin/README.md) is ready in your workspace.

To deploy it to your GitHub profile, push it to a repo named **`Shikhar-395`** (same as your username). If you haven't created that repo yet:

1. Create a new repo on GitHub named `Shikhar-395` (public, with no template)
2. Then push:
```bash
git init
git add README.md
git commit -m "Add profile README"
git remote add origin git@github.com:Shikhar-395/Shikhar-395.git
git push -u origin main
```

The pie chart, badges, and social links will all render natively on GitHub. 🚀